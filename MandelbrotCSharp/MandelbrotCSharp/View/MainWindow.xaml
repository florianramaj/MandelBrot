<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MandelbrotCSharp"
        x:Class="MandelbrotCSharp.View.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Grid.RowSpan="3" Command="{Binding CalculateCommand}" Height="50">Calculate</Button>
        <Image Grid.RowSpan="3" Grid.Column="2" Source="{Binding Path=Image, Mode=OneWay}"></Image>
        <Label Grid.Row="0" Grid.Column="1" Content="{Binding Path=Stopwatch}"></Label>
        <TextBox Grid.Row="1" Height="30" Grid.Column="1" Text="{Binding Path=Height}"></TextBox>
        <TextBox Grid.Row="2" Height="30" Grid.Column="1" Text="{Binding Path=Width}"></TextBox>
    </Grid>
</Window>
